# Tooling

Shared CMake and test support for future learning modules.

Files in this directory are intentionally minimal:

- `cmake/AddLearningModule.cmake` provides a small helper for registering a module and its tests.
- `tests/` contains reusable test support targets.
- `scripts/` contains convenience wrappers for configure, build, and test commands.
