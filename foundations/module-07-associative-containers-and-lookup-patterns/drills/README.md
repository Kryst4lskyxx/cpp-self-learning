# Drills

`frequency_map.h` is a small lookup exercise.

Build a frequency map for a sequence of words and focus on the container choice: the index should make repeated updates cheap, and the lookup should stay simple and predictable.

The point is to practice recognizing when an unordered associative container is the right default and when a sorted result still needs an explicit ordered step.
