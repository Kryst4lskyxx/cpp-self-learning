# Drill

`value_selector.h` is a warm-up for selector-driven generic code.

Implement a helper that accepts a range and a caller-provided callable, then returns the projected values in a `std::vector`. Keep the interface explicit about the callable input and the output element type.

The goal is to practice writing a small template that exposes its assumptions clearly before you move on to the assignment.

