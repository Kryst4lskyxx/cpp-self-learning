# Expert

The useful habit here is to recognize when a custom loop is only hiding a standard pipeline.

If your code is "read every item, transform it, sort it, remove duplicates," you usually want the standard algorithms to say that directly.
