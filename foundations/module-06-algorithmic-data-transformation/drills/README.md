# Drills

`normalize_words.h` is a small transformation exercise.

Take a range of words and normalize them into a consistent lowercase form before you apply any ordering or deduplication.

The point is to make the first stage of the pipeline feel mechanical enough that the later `sort` and `unique` steps read naturally.
