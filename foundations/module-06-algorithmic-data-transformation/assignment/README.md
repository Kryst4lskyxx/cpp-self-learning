# Assignment

Implement `normalize_and_unique` in `pipeline.h` so it normalizes a list of words, sorts them, and removes duplicates.

The test expects the helper to return lowercase words in alphabetical order with repeated values removed.

Use a small algorithm pipeline. The goal is to practice expressing a standard transformation chain with `transform`, `sort`, and `unique`, not to build a custom container or parser.
