# Drills

The drill for this module is `hello_cli.cpp`.

Build and run it to confirm the toolchain works before touching the assignment.

This is a quick confidence check, not the assignment:

- build the `module_01_drill` target with CMake
- run the drill executable from the build tree
- use it to confirm the compiler, linker, and local environment are all working before you start the test-driven assignment
