# Module 01: Tooling and First Program

This module introduces the smallest complete C++ workflow in the repo:

- build a single executable with CMake
- run a tiny drill program from the command line
- verify the assignment with an automated test

Start with the lesson, then run the drill, then complete the assignment.
