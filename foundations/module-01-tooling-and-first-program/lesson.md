# Lesson

This module is about getting one C++ program from source to a runnable binary.

What to notice:

- CMake creates the build targets
- the drill is a quick local experiment
- the assignment is the version the test expects
- the test launches the assignment executable and checks its output

Keep the first pass small. One source file, one binary, one expected line of output.
