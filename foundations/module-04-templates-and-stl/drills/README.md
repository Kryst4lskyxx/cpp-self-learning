# Drills

`range_utils.h` is a small iterator exercise.

The goal is to practice a template helper that accepts a begin/end pair, applies a predicate, and returns the matching values in a container. Keep it simple enough that you can read the whole implementation in one glance.

That is the same filtering idea as the assignment, just expressed one level lower with raw iterators instead of a whole range object.
