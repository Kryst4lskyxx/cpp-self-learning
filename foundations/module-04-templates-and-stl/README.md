# Module 04: Templates and STL

This module introduces the C++ generic programming model:

- templates let you write one algorithm that works for many value types
- standard algorithms such as `std::copy_if` separate the loop from the logic
- iterators and inserters let containers participate in those algorithms
- predicates make behavior configurable without building a class hierarchy

Start with the lesson, try the drill, then implement the assignment from the test.
