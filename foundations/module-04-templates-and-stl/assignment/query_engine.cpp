#include "query_engine.h"

// The filtering algorithm is a template, so the implementation lives in the header.
// This translation unit keeps the assignment as a normal library target for CMake.
int module_04_assignment_anchor() {
    return 0;
}
