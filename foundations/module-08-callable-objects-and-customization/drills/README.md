# Drills

`threshold_filter.h` is a small predicate exercise.

Take a range of values and return the ones that pass a threshold check. The useful part is not the threshold itself, but the fact that the check can be expressed as a callable that the algorithm invokes for each value.

Keep the helper tiny. The point is to get comfortable with "pass behavior in, keep the loop generic" before you move on to the ranking assignment.
