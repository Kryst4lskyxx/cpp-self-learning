# Drills

`threshold_filter.h` is a small comparator exercise.

Take a range of values and return the ones that compare as a match against a threshold. The drill keeps the API narrow on purpose: a threshold value plus a comparator callable that decides whether each element passes.

Keep the helper tiny. The point is to get comfortable with "pass behavior in, keep the loop generic" before you move on to the ranking assignment.
