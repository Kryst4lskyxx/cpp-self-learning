# Module 03: Classes and Const Correctness

This module introduces the first C++ object model pattern in the curriculum:

- keep data in a class with private state
- expose behavior through member functions
- mark observers `const` so they work on const objects
- return read-only views when a caller should inspect, not mutate

Start with the lesson, then try the drill, then implement the assignment from the test.
