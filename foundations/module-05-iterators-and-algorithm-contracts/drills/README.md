# Drills

`range_distance.h` is a small iterator exercise.

Measure the size of a half-open range from iterators, then reuse that helper for a whole range if you want a slightly more ergonomic call site.

The point is to get comfortable with the `[first, last)` shape before you apply the same thinking to algorithm contracts.
